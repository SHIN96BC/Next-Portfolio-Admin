type Category {
  id: ID!
  name: String!
}

type Product {
  id: ID!
  name: String!
  description: String
  price: Int!
  currency: String!
  inStock: Boolean!
  rating: Float
  thumbnailUrl: String
  images: [String!]
  category: Category
  createdAt: DateTime!
  updatedAt: DateTime
}
